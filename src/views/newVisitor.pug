html
head
   title Person
   body
      form(action = "/person", method = "POST")
      div
         label(for = "name") visitor_name: 
         input(name = "name")
      br
      div
         label(for = "age") visitor_age: 
         input(name = "age")
      br
      div
         label(for = "nationality") Nationality: 
         input(name = "nationality")
      br
      button(type = "submit") Create new person